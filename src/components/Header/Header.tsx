import { FC } from 'react';
import './Header.scss';

/**
 * Header of the app
 *
 * @return {JSX.Element}
 */
const Header: FC = () => {
    return <header className="header"></header>;
};

export default Header;
